# Sample: Zookeeper single node

Tool: Docker Compose

## How to run it

```bash
docker-compose up -d
```

This will create a container exposing port 2181 on localhost
and storing information in `zoo/logs` directory.